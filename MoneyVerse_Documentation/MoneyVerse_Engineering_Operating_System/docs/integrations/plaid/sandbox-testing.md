# Sandbox Testing

Plaid is disabled by default.

When enabled, only an authenticated adult may initiate connection. Use server-side token exchange, encrypted storage, verified webhooks, idempotent sync, data minimization, and easy disconnection.

Never collect banking credentials directly.
